name: CD - Pipeline de Despliegue

on:
  push:
    branches: [ main ]

jobs:
  # ------------------------------------------------------------------------------
  # 1. AUDIT SECRETS: Validar claves de seguridad
  # ------------------------------------------------------------------------------
  audit_secrets:
    name: "🔐 Audit: Secrets"
    runs-on: ubuntu-latest
    environment: prod
    steps:
    - name: "Validar Clave SSH"
      env:
        SSH_KEY: ${{ secrets.SERVER_SSH_KEY }}
      run: |
        if [[ -z "$SSH_KEY" ]]; then
          echo "::error title=Secret Missing::La clave SERVER_SSH_KEY no está cargada."
          exit 1
        fi

  # ------------------------------------------------------------------------------
  # 2. AUDIT VARIABLES: Validar configuración de entorno
  # ------------------------------------------------------------------------------
  audit_variables:
    name: "📋 Audit: Variables"
    runs-on: ubuntu-latest
    environment: prod
    steps:
    - name: "Validar Variables de Entorno"
      run: |
        MISSING=()
        [[ -z "${{ vars.REMOTE_HOST }}" ]] && MISSING+=("REMOTE_HOST")
        [[ -z "${{ vars.REMOTE_USER }}" ]] && MISSING+=("REMOTE_USER")
        [[ -z "${{ vars.REMOTE_TARGET }}" ]] && MISSING+=("REMOTE_TARGET")
        
        if [ ${#MISSING[@]} -ne 0 ]; then
          echo "::error title=Vars Missing::Faltan: ${MISSING[*]}"
          exit 1
        fi

  # ------------------------------------------------------------------------------
  # 3. HOST DETECTION: ¿Es IP o Dominio?
  # ------------------------------------------------------------------------------
  host_type:
    name: "🔍 Audit: Host Type"
    needs: audit_variables
    runs-on: ubuntu-latest
    environment: prod
    outputs:
      type: ${{ steps.detect.outputs.type }}
    steps:
    - id: detect
      run: |
        HOST="${{ vars.REMOTE_HOST }}"
        if [[ "$HOST" =~ ^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
          echo "type=ip" >> $GITHUB_OUTPUT
          echo "::notice title=Host Type::Se detectó una IP directa. Saltando validación DNS."
        else
          echo "type=domain" >> $GITHUB_OUTPUT
          echo "::notice title=Host Type::Se detectó un nombre de dominio. Iniciando validación DNS."
        fi

  # ------------------------------------------------------------------------------
  # 4. DNS RESOLUTION: Solo si es dominio
  # ------------------------------------------------------------------------------
  dns_resolution:
    name: "🌐 Audit: DNS Resolution"
    needs: host_type
    if: needs.host_type.outputs.type == 'domain'
    runs-on: ubuntu-latest
    environment: prod
    steps:
    - name: "DNS Lookup"
      run: |
        HOST="${{ vars.REMOTE_HOST }}"
        echo "🔍 Intentando resolver: $HOST"
        if ! nslookup "$HOST"; then
          echo "::error title=DNS Error::El dominio '$HOST' no existe o no tiene un registro A configurado. TIP: Verifica los registros DNS en tu proveedor (Cloudflare/VPS)."
          exit 1
        fi

  # ------------------------------------------------------------------------------
  # 5. SSH CHECK: ¿El servidor acepta la conexión?
  # ------------------------------------------------------------------------------
  ssh_check:
    name: "🔑 Audit: SSH Auth"
    needs: [host_type, dns_resolution]
    if: always() && (needs.host_type.result == 'success') && (needs.dns_resolution.result == 'success' || needs.dns_resolution.result == 'skipped')
    runs-on: ubuntu-latest
    environment: prod
    steps:
    - name: "Prueba de Conexión"
      uses: appleboy/ssh-action@master
      with:
        host: ${{ vars.REMOTE_HOST }}
        username: ${{ vars.REMOTE_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: ${{ vars.REMOTE_PORT || '22' }}
        script: echo "✅ SSH Handshake exitoso."

  # ------------------------------------------------------------------------------
  # 6. DEPLOY: Sincronización de archivos
  # ------------------------------------------------------------------------------
  deploy:
    name: "🚀 Sincronizar Archivos"
    needs: ssh_check
    runs-on: ubuntu-latest
    environment: prod
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: "🚀 Deploy via SSH/Rsync"
      uses: easingthemes/ssh-deploy@main
      with:
        SSH_PRIVATE_KEY: ${{ secrets.SERVER_SSH_KEY }}
        REMOTE_HOST: ${{ vars.REMOTE_HOST }}
        REMOTE_USER: ${{ vars.REMOTE_USER }}
        REMOTE_PORT: ${{ vars.REMOTE_PORT || '22' }}
        TARGET: ${{ vars.REMOTE_TARGET }}
        ARGS: "-avz --delete"
        EXCLUDE: "/.git/, /.github/, /node_modules/, /tests/, /phpunit.xml, /phpstan.neon, /phpcs.xml"

  # ------------------------------------------------------------------------------
  # 7. VERIFY: Post-despliegue y Salud
  # ------------------------------------------------------------------------------
  verify:
    name: "🏥 Verificar Salud"
    needs: deploy
    runs-on: ubuntu-latest
    environment: prod
    steps:
    - name: "🧹 Post-Deploy & Summary"
      uses: appleboy/ssh-action@master
      with:
        host: ${{ vars.REMOTE_HOST }}
        username: ${{ vars.REMOTE_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: ${{ vars.REMOTE_PORT || '22' }}
        script: |
          cd ${{ vars.REMOTE_TARGET }}
          if command -v wp &> /dev/null; then
            wp cache flush --allow-root
            echo "✅ WordPress Cache Flushed"
          fi
          echo '{"commit": "${{ github.sha }}", "date": "$(date -u)"}' > deploy-info.json

    - name: "🏥 Smoke Test"
      run: |
        RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" ${{ vars.SITE_URL }})
        if [ "$RESPONSE" -eq 200 ]; then
          echo "✅ Site is UP"
        else
          echo "::error title=Site Down::El sitio respondió con HTTP $RESPONSE"
          exit 1
        fi

    - name: "📊 Final Summary"
      run: |
        echo "### 🚀 Reporte Final de Despliegue" >> $GITHUB_STEP_SUMMARY
        echo "| Etapa | Resultado |" >> $GITHUB_STEP_SUMMARY
        echo "| :--- | :--- |" >> $GITHUB_STEP_SUMMARY
        echo "| 🔐 Seguridad | ✅ OK |" >> $GITHUB_STEP_SUMMARY
        echo "| 🔍 Tipo de Host | ${{ needs.host_type.outputs.type == 'ip' && '📌 IP Directa' || '🌐 Dominio' }} |" >> $GITHUB_STEP_SUMMARY
        echo "| 🌐 Resolución DNS | ${{ needs.dns_resolution.result == 'skipped' && '⏭️ Saltada' || '✅ Exitosa' }} |" >> $GITHUB_STEP_SUMMARY
        echo "| 🔑 Acceso SSH | ✅ OK |" >> $GITHUB_STEP_SUMMARY
        echo "| 📦 Despliegue | ✅ OK |" >> $GITHUB_STEP_SUMMARY
